import { createTheme } from "@mui/material";

export const DatepickerTheme = createTheme({
  palette: {
    primary: {
      main: '#66B4DF',
      light: '#66B4DF',
      dark: '#66B4DF',
      contrastText: '#fff',
    }
  },
});